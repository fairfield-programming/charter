/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   runtime: 'experimental-edge',
  // },
  swcMinify: true,
}

module.exports = nextConfig
